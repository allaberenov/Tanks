import pygame

pygame.init()
WIDTH, HEIGHT = 800, 500
window = pygame.display.set_mode((WIDTH, HEIGHT))

# def create_window():
#    width, height = 800, 600
#    window = pygame.display.set_mode((width, height))
#    return window
